# It defines the host of the Notification server.
notification.openapi.client.host=http://localhost:9090

# It defines the base path of the Notification server.
notification.openapi.client.base-path=${notification.openapi.client.host}
