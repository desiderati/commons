{
  "name": "pringbloom",
  "version": "4.0.0.RC9",
  "scripts": {
    "mvn:deploy": "mvn -B -e -fn -Dmaven.wagon.http.pool=false clean deploy"
  },
  "engines": {
    "node": ">=12.22.12"
  },
  "keywords": [
    "node",
    "maven"
  ]
}
