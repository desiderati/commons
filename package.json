{
  "name": "commons-herd.io",
  "version": "2.5.2",
  "scripts": {
    "mvn:deploy": "mvn -B -e -fn -Dmaven.wagon.http.pool=false clean deploy"
  },
  "engines": {
    "node": ">=12.13.10"
  },
  "keywords": [
    "node",
    "maven"
  ]
}
